---
name: Check Collection

on:
  schedule:
    - cron: '0 21 * * *'

  workflow_dispatch:

permissions: write-all

defaults:
  run:
    shell: bash

jobs:

  build:
    name: Create package collection
    runs-on: ubuntu-latest
    env:
      PACK_DIR: /root/.pack
    strategy:
      fail-fast: false
    container: ghcr.io/stefan-hoeck/idris2-pack:latest
    steps:
      - name: Install dependencies
        run: |
          apt-get update
          apt-get install --yes libgsl-dev
          apt-get install --yes libpq-dev
      - name: Use latest pack commit
        run: pack update
      - name: Use latest HEAD.toml
        run: pack update-db
      - name: Install latest pack-admin
        run: |
          echo '[custom.all.pack-admin]' >> "pack.toml"
          echo 'type   = "github"' >> "pack.toml"
          echo 'url    = "https://github.com/stefan-hoeck/idris2-pack"' >> "pack.toml"
          echo 'commit = "latest:main"' >> "pack.toml"
          echo 'ipkg   = "pack-admin.ipkg"' >> "pack.toml"
          pack --no-prompt install-app pack-admin
      - name: Extract Collection from HEAD
        run: pack-admin --no-prompt extract-from-head "$PACK_DIR/db/testdb.toml"
      - name: Check Collection
        run: |
          mkdir "$HOME/output"
          pack-admin --no-prompt check-collection testdb "$HOME/output/STATUS.md"
      - name: Checkout
        uses: actions/checkout@v3
        if: always()
        with:
          persist-credentials: false
          fetch-depth: 0
      - name: Add STATUS.md
        if: always()
        run: |
          cp "$HOME/output/STATUS.md" STATUS.md
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add STATUS.md
      - name: Git add collection
        run: |
          cp "$HOME/.pack/db/testdb.toml" "collections/nightly-$(date +%y%m%d).toml"
          git add collections/*.toml
      - name: Git commit
        if: always()
        run: git commit -m "[ check ] collection update" -m "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"
      - name: Push changes
        if: always()
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}
